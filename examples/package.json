{
  "name": "cognito_user_pool",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "all": "yarn default && yarn attributes && yarn invite_messages && yarn mfa_verifications && yarn policies && yarn users_and_groups",
    "attributes": "cd attributes && yarn start",
    "default": "cd default && yarn start",
    "invite_messages": "cd default && yarn start",
    "mfa_verifications": "cd mfa_verifications && yarn start",
    "policies": "cd policies && yarn start",
    "users_and_groups": "cd users_and_groups && yarn start"
  }
}
